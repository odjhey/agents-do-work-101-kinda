Try to make and evaluate some prompt chains + RAG + other llm shenanigans.

## Setup

```bash
cp .env-template .env
```

## Run

See the `src` dir

## Eval

See the `eval` dir

## Scripts

See the `package.json` scripts

## Structure

- `src/` - Agent graph + `/api`
- `rag/` - Vector DB setup
- `eval/` - Evaluation scripts
- `infra/` - Docker compose files
- `docs/` - Don't trust, always outdated
